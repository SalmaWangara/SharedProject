const login = document.getElementById('Log-in');
const all = document.getElementById('hero');

login.addEventListener(onClick, () => {
  all.style.display = block;
});


const hospital = document.querySelector('div.hospitals');
const cart = document.querySelector('span.cart');

hospital.addEventListener(onClick, () => {
    
})